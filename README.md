# neko-words
Neko-Words is a minimal vocabulary tool. Add words via CLI; AI writes translations and examples. Review anytime in a simple React web UI with spaced repetition. Built with Python + React.
